# 📘 คู่มือเว็บไซต์ชมรมมุสลิม มหาวิทยาลัยขอนแก่น

คู่มือนี้สำหรับ **ผู้ดูแลเว็บ** (ไม่ต้องเก่งโค้ดก็ทำตามได้)

---

## 1. โครงสร้างไฟล์ (อะไรอยู่ตรงไหน)

```
Muslim Club Website/
├── index.html              ← หน้าเว็บหลัก (โครงหน้าตา)
├── team.html               ← หน้าทีมงาน (รวมฟอร์มกรอกข้อมูล — ดูข้อ 6)
├── manifest.webmanifest    ← ตั้งค่า "ติดตั้งเป็นแอป" (ไม่ต้องแตะ)
├── sw.js                   ← ตัวทำให้เปิดได้ตอนเน็ตหลุด (ไม่ต้องแตะ)
├── css/style.css           ← สไตล์/ตกแต่ง
├── js/
│   ├── config.js   ⭐       ← ไฟล์เดียวที่คุณต้องแก้ (ใส่ลิงก์ Google)
│   ├── config.example.js   ← ไฟล์ตัวอย่างของ config (แม่แบบ ไม่ต้องแตะ)
│   ├── sheets.js           ← ตัวช่วยอ่าน Google ชีต (ไม่ต้องแตะ)
│   ├── main.js             ← สลับหน้า/เมนู (ไม่ต้องแตะ)
│   ├── content.js          ← ข้อความหน้าแรก (ดึงจาก config — ไม่ต้องแตะ)
│   ├── prayer.js           ← เวลาละหมาด (ไม่ต้องแตะ)
│   ├── documents.js        ← คลังเอกสาร
│   ├── knowledge.js        ← คลังความรู้ (สื่อการเรียนรู้ศาสนา)
│   ├── events.js           ← ตารางกิจกรรม (timeline + ติดตามงานแต่ละฝ่าย)
│   ├── finance.js          ← การเงิน
│   ├── announcements.js    ← ประกาศกิจกรรม
│   ├── committee.js        ← ทำเนียบคณะกรรมการ
│   ├── contact.js          ← หน้าติดต่อ + ฟอร์มสมัครสมาชิก
│   └── viewer.js           ← ตัวเปิดอ่าน PDF/รูป/วิดีโอ ในเว็บ (ไม่ต้องแตะ)
├── assets/img/             ← รูปภาพ + ไอคอนแอป
├── google-sheets-templates/← เทมเพลตชีต + วิธีทำฟอร์ม (อ่านประกอบ)
├── google-apps-script/     ← ระบบแจ้งเตือนเข้ากลุ่มอัตโนมัติ (ดูข้อ 9)
├── docs/คู่มือส่งมอบรุ่น.md  ← เช็คลิสต์ส่งมอบกรรมการรุ่นถัดไป + วิธีจดโดเมน
└── คู่มือ-README.md         ← ไฟล์นี้
```

> 🔑 **จำแค่ไฟล์เดียว:** ถ้าจะ "เชื่อม Google" ให้แก้ที่ **`js/config.js`** เท่านั้น
> ไฟล์อื่นที่เขียนว่า "ไม่ต้องแตะ" คือระบบหลังบ้าน ปล่อยไว้ได้เลย

---

## 2. วิธีเปิดดูเว็บในเครื่อง

เพราะเว็บแยกเป็นหลายไฟล์แล้ว ต้องเปิดผ่านเซิร์ฟเวอร์เล็ก ๆ (ดับเบิลคลิกเฉย ๆ จะไม่ทำงานครบ)

1. เปิด Terminal/PowerShell ในโฟลเดอร์นี้
2. พิมพ์:  `python -m http.server 8000`
3. เปิดเบราว์เซอร์ไปที่:  **http://localhost:8000**
4. เลิกใช้กด `Ctrl + C`

---

## 3. ⭐ วิธีเชื่อม Google (หัวใจของระบบ)

**แนวคิด:** คนในชมรมแค่ "กรอก Google ฟอร์ม" → ข้อมูลเข้า "Google ชีต" → เว็บดึงมาแสดงเอง
ไม่ต้องแตะโค้ดเลย

```
กรอกฟอร์ม (มือถือ)  →  Google ชีต  →  เว็บอัปเดตอัตโนมัติ
```

### ขั้นตอนทำ 1 ระบบ (ใช้กับเอกสาร/การเงิน/ประกาศ เหมือนกันหมด)

**ก. สร้างฟอร์มสำหรับกรอก**
1. ไปที่ https://forms.google.com สร้างฟอร์มใหม่
2. ใส่ช่องกรอกตามที่ระบบนั้นต้องการ (ดูตารางหัวข้อ 4)
3. กดแท็บ **"การตอบกลับ" (Responses)** → ไอคอนชีตเขียว → สร้างชีต
   (ตอนนี้ทุกครั้งที่มีคนกรอกฟอร์ม ข้อมูลจะลงชีตอัตโนมัติ)

**ข. เผยแพร่ชีตให้เว็บอ่านได้**
1. เปิดชีตที่เพิ่งสร้าง → เมนู **ไฟล์ > แชร์ > เผยแพร่ไปยังเว็บ**
2. เลือกแผ่นงานที่ใช่ และรูปแบบ **"ค่าที่คั่นด้วยจุลภาค (.csv)"**
3. กด **เผยแพร่** แล้ว **คัดลอกลิงก์** ที่ได้

**ค. วางลิงก์ลงเว็บ**
- เปิดไฟล์ `js/config.js` วางลิงก์ชีต (.csv) ในช่อง `sheets:` ที่ตรงกับระบบ
- วางลิงก์ฟอร์ม (ปุ่ม "ส่ง" ของฟอร์ม) ในช่อง `forms:` ที่ตรงกัน
- เซฟไฟล์ → รีเฟรชเว็บ ✅

> ✅ **ทุกหน้าใช้งานได้แล้ว** (คลังเอกสาร · การเงิน · ประกาศ · คลังความรู้ · ตารางกิจกรรม · ทำเนียบกรรมการ) — แค่วางลิงก์ใน `config.js` ข้อมูลจริงก็แสดงทันที
> (การเงินคำนวณยอดรวม/กราฟ/โดนัท/ตารางให้อัตโนมัติ, ประกาศขึ้นเป็นการ์ดบนหน้าแรก, คลังความรู้เปิดอ่าน PDF ในเว็บได้)
> 💡 **ไม่จำเป็นต้องมีฟอร์มทุกอัน** — ของที่แก้นานๆครั้ง (ตารางกิจกรรม/ทำเนียบกรรมการ) พิมพ์ลงชีตตรง ๆ ได้เลย สะดวกกว่า

---

## 4. โครงหัวข้อ (คอลัมน์) ของแต่ละชีต

ตั้งชื่อหัวคอลัมน์ในฟอร์ม/ชีต **ให้ตรงตามนี้เป๊ะ ๆ**

> ✅ **ระบบอนุมัติก่อนขึ้นเว็บ (ใช้ได้ทุกชีต ไม่บังคับ):** เพิ่มคอลัมน์ชื่อ **`อนุมัติ`** ต่อท้ายชีต
> แถวจะขึ้นเว็บก็ต่อเมื่อกรรมการพิมพ์ **ใช่** (หรือใช้ช่องติ๊กถูก ☑ ของชีต) — ของที่กรอกฟอร์มมาใหม่จะ "รอตรวจ" ไม่โผล่บนเว็บจนกว่าจะติ๊ก
> ชีตไหน**ไม่มี**คอลัมน์นี้ = ทุกแถวขึ้นเว็บทันทีตามเดิม

**คลังเอกสาร** (`sheets.documents`) — จัดเก็บตามปีการศึกษา / เทอม / โครงการ
| ชื่อเอกสาร | ปีการศึกษา | เทอม | โครงการ | ประเภทไฟล์ | ขนาด | วันที่ | ลิงก์ |
|---|---|---|---|---|---|---|---|
| เอกสารโครงการละศีลอด | 2569 | 2 | โครงการละศีลอดสัมพันธ์ | PDF | 2.4 MB | 2 เม.ย. 2569 | (ลิงก์) |

> เว็บจะ **จัดกลุ่มเอกสารตามโครงการ** อัตโนมัติ และมีตัวเลือก *ปีการศึกษา / เทอม* ให้เลือกดู — เหมาะกับการเก็บเป็นตัวอย่างส่งต่อรุ่นน้อง
> `ปีการศึกษา`/`เทอม`/`โครงการ` เว้นว่างได้ (จะถูกจัดเข้ากลุ่ม "เอกสารทั่วไป")
>
> ✨ **ไม่ต้องกรอกเองก็ได้:**
> - **`ประเภทไฟล์`** — เว้นว่างได้ เว็บเดาจากนามสกุลในลิงก์ให้เอง (`.pdf` → PDF, `.docx` → Word ฯลฯ)
> - **`ขนาด`** — เว้นว่างได้ (ไม่กรอกก็ไม่แสดง)
> - **`วันที่`** — เว้นว่างได้ ถ้าใช้ Google ฟอร์ม เว็บจะใช้คอลัมน์ **"ประทับเวลา"** ของฟอร์มแทนอัตโนมัติ
> - **`ลิงก์`** — ถ้าใช้ฟอร์มแบบ **อัปโหลดไฟล์** เว็บจะจับลิงก์ไฟล์จากฟอร์มให้เอง (ตั้งชื่อคอลัมน์ว่าอะไรก็ได้)
>
> 👉 สรุป: ถ้าทำเป็น Google ฟอร์ม ผู้กรอกใส่แค่ **ชื่อเอกสาร + โครงการ (+ ปี/เทอม) + แนบไฟล์** ก็พอ

**การเงิน** (`sheets.finance` / `financeYears`) — แยกตามโครงการ + ดูเป็นปฏิทินรายวันได้
| วันที่ | ประเภท | หมวดหมู่ | โครงการ | รายละเอียด | ผู้รับผิดชอบ | ราคา | หลักฐาน |
|---|---|---|---|---|---|---|---|
| 1 เม.ย. 2569 | รายจ่าย | อาหาร | โครงการรอมฏอน | ค่าอาหารละศีลอด | มหาธีร์ | 4200 | (ลิงก์ใบเสร็จ) |

> หมวดหมู่ = เป็นค่าอะไร (อุปกรณ์/อาหาร/บริจาค) · โครงการ = ของงานไหน (โครงการรอมฏอน/งานทั่วไป)
> `ผู้รับผิดชอบ` (ผู้รับ/ผู้เบิก) · `หลักฐาน` (ลิงก์รูปใบเสร็จ/ไดรฟ์ — กดปุ่ม 🧾 ดูได้) เว้นว่างได้
> รองรับชื่อคอลัมน์เก่า `รายการ`/`จำนวน` ด้วย (ถ้าชีตเดิมยังใช้)

> ใส่คอลัมน์ `โครงการ` แล้วเว็บจะ **สรุปยอดรับ/จ่ายแยกแต่ละโครงการ** ให้เอง
> หน้าการเงินมี **ปฏิทินรายวัน** — แตะวันที่เพื่อดูรายการของวันนั้น (เหมาะกับช่วงรอมฎอนที่มีรับ-จ่ายทุกวัน) เริ่มต้นเปิดที่เดือนซึ่งมีรายการมากที่สุด

**ประกาศกิจกรรม** (`sheets.announcements`)
| หัวข้อ | วันที่ | หมวดหมู่ | รายละเอียด | รูปภาพ | ลิงก์ |
|---|---|---|---|---|---|
| ค่ายอบรมเยาวชน | 20 มิ.ย. 2569 | กิจกรรม | รับสมัคร...​ | (ลิงก์รูป) | (ลิงก์อ่านต่อ) |

**ตารางกิจกรรม** (`sheets.events`) — Timeline + นับถอยหลัง + ติดตามงานแต่ละฝ่าย
> 💡 ตารางนี้แนะนำให้ **พิมพ์ลงชีตตรง ๆ** (ไม่ต้องใช้ฟอร์ม) เพราะมีการติ๊กสถานะ/ใส่เดดไลน์บ่อย แก้ในชีตสะดวกกว่า

| ชื่อกิจกรรม | วันที่ | สถานที่ | รายละเอียด | ฝ่าย | หน้าที่ | เดดไลน์ | สถานะ |
|---|---|---|---|---|---|---|---|
| ค่ายอบรม First Step | 25/6/2026 | อาคารกิจกรรม | ค่ายพัฒนาผู้นำ | ฝ่ายวิชาการ | เตรียมเนื้อหา | 15/6/2026 | เสร็จ |
| ค่ายอบรม First Step | 25/6/2026 | | | ฝ่ายวิชาการ | ติดต่อวิทยากร | 18/6/2026 | กำลังทำ |
| ค่ายอบรม First Step | 25/6/2026 | | | ฝ่ายกิจกรรม | จัดสถานที่ | 22/6/2026 | ยัง |

> **1 แถว = 1 งานของฝ่าย** → ชื่อกิจกรรมเดียวกันใส่หลายแถวได้ เว็บจับกลุ่มเอง (วันที่/สถานที่ ใส่แถวไหนก็ได้)
> **ฝ่ายเดียวมีหลายหน้าที่** = ใส่หลายแถว (ฝ่ายเดิม หน้าที่ใหม่) → เว็บโชว์ชื่อฝ่ายครั้งเดียว แล้วไล่หน้าที่เป็นข้อ ๆ ใต้ฝ่าย
> `เดดไลน์` = วันที่ฝ่ายนั้นต้องเตรียม "หน้าที่" นี้ให้เสร็จ (รูปแบบ `28/6/2026`) — เว้นว่างได้
>   เว็บโชว์ป้ายนับถอยหลังของแต่ละหน้าที่: **เลยกำหนด/วันนี้ = แดง · เหลือ ≤3 วัน = ส้ม · งานเสร็จแล้ว = สีจาง**
> `สถานะ` ใส่: **เสร็จ / กำลังทำ / ยัง** · เว็บแยก "กำลังจะมาถึง" (นับถอยหลัง) กับ "ผ่านไปแล้ว" อัตโนมัติจากวันที่

**คลังความรู้** (`sheets.knowledge`) — สื่อการเรียนรู้ศาสนา (สรุปฮาลาเกาะห์/หนังสือ/วิดีโอ/เสียง)
| ชื่อเรื่อง | หมวด | ผู้บรรยาย | รายละเอียด | ลิงก์ | PDF | รูปปก | วันที่ |
|---|---|---|---|---|---|---|---|
| สรุปฮาลาเกาะห์: หลักศรัทธา | สรุปฮาลาเกาะห์ | อ.อับดุลเลาะห์ | สรุปสาระสำคัญ... | (ลิงก์) | (แนบไฟล์) | (ลิงก์รูป) | 10 มิ.ย. 2569 |

> หมวดที่มีไอคอน/ปุ่มพิเศษ: `สรุปฮาลาเกาะห์` `หนังสือ` `บทความ` `วิดีโอ` `เสียงบรรยาย` (หมวดอื่นใช้ไอคอนเริ่มต้น)
> `ผู้บรรยาย`/`รายละเอียด`/`รูปปก`/`วันที่` เว้นว่างได้
> 📎 **ใส่สื่อได้ 2 ทาง (เลือกอย่างใดอย่างหนึ่ง):** `ลิงก์` (ยูทูบ/เว็บ/ไดรฟ์) **หรือ** `PDF` (แนบไฟล์ผ่านฟอร์ม) — ถ้ามีทั้งคู่ เว็บใช้ไฟล์ PDF ก่อน
> 📖 **กดปุ่มอ่าน/ดู = เปิดในเว็บเลย** (PDF/รูป/วิดีโอ อ่านในกรอบป็อปอัป) + มีปุ่ม **ดาวน์โหลด** — ไม่เด้งออกจากเว็บ
> ⚠️ ไฟล์ PDF ที่อัปผ่านฟอร์ม ต้อง **ตั้งโฟลเดอร์ไดรฟ์ของฟอร์มเป็น "ทุกคนที่มีลิงก์" 1 ครั้ง** ไม่งั้นคนนอกเปิด/ดาวน์โหลดไม่ได้

**ทำเนียบคณะกรรมการ** (`sheets.committee`) — เปลี่ยนรายชื่อกรรมการได้ทุกปีโดยไม่ต้องแก้โค้ด
| ชื่อ | ตำแหน่ง | เบอร์โทร | อีเมล | เฟซบุ๊ก | รูปภาพ |
|---|---|---|---|---|---|
| อ.อับดุลเลาะห์ ยูซุฟ | ประธานชมรม | 096-xxx-xxxx | a@kku.ac.th | (ลิงก์เฟซบุ๊ก) | (ลิงก์รูปไดรฟ์) |

> `เบอร์โทร`/`อีเมล`/`เฟซบุ๊ก`/`รูปภาพ` เว้นว่างได้ — ไอคอนที่ไม่มีข้อมูลจะถูกซ่อน
> กด 📞/✉️ = โชว์+คัดลอกเบอร์/อีเมล · กด 📘 = เปิดเฟซบุ๊กของกรรมการ (ใส่ลิงก์เพจ/โปรไฟล์เต็ม)
> `รูปภาพ` เว้นว่าง = ใช้ตัวอักษรแรกของชื่อบนวงกลมไล่สีแทน (ใส่ลิงก์รูปไดรฟ์ที่แชร์ "ทุกคนที่มีลิงก์" ได้)

> หมวดหมู่ประกาศที่มีไอคอน/สีพิเศษ: `ละหมาด`, `การกุศล`, `เรียนรู้`, `กิจกรรม` (หมวดอื่นใช้ไอคอนลำโพงเริ่มต้น)
>
> 🖼️ **วิธีใส่รูปในคอลัมน์ `รูปภาพ`:**
> 1. อัปรูปขึ้น **Google ไดรฟ์** → คลิกขวาที่รูป → **แชร์ → "ทุกคนที่มีลิงก์"** → **คัดลอกลิงก์** → วางในช่อง `รูปภาพ`
>    (เว็บแปลงลิงก์ไดรฟ์ให้แสดงเป็นรูปได้เองอัตโนมัติ — ⚠️ ต้องตั้งแชร์เป็น "ทุกคนที่มีลิงก์" ไม่งั้นรูปไม่ขึ้น)
> 2. หรือใช้ **ลิงก์รูปตรง** ที่ลงท้าย `.jpg`/`.png` จากที่ไหนก็ได้ วางได้เลย
> 3. **เว้นว่าง** = ใช้พื้นหลังไล่สี + ไอคอนตามหมวดให้อัตโนมัติ (สวยอยู่แล้ว)
>
> คอลัมน์ `ลิงก์` (ปุ่ม "อ่านต่อ") เว้นว่างได้

---

## 5. ค่าที่ปรับได้ใน `config.js`

- **เวลาละหมาด** — `prayer.method` (วิธีคำนวณ), `prayer.school` (0=ชาฟิอี/1=ฮานาฟี),
  `prayer.jumuah` (เวลาญุมอะฮ์ของชมรม) — ปกติเวลาละหมาดดึงสดจากอินเทอร์เน็ตให้อัตโนมัติทุกวัน
- **ข้อมูลติดต่อ** — `contact.address / phone / email` และโซเชียล `contact.facebook / line / instagram / youtube`
  (ช่องโซเชียลที่เว้นว่าง ไอคอนจะถูกซ่อนให้อัตโนมัติ)
- **ฟอร์มสมัครสมาชิก** — ใส่ลิงก์ฟอร์มในช่อง `forms.membership` แล้วแบบฟอร์มจริงจะ **ฝังแสดงในหน้าติดต่อ** ทันที
  (ใช้ลิงก์ "ส่ง" ของฟอร์ม หรือลิงก์ `.../viewform` ก็ได้ ระบบเติม `embedded=true` ให้เอง)
- **ข้อความหน้าแรก** — บล็อก `content`: `clubName` (ชื่อชมรม), `heroTitle1`/`heroTitle2` (สโลแกน 2 บรรทัด),
  `heroText` (ย่อหน้าแนะนำ), `stats` (สถิติ 3 ช่อง — แก้ตัวเลข/คำได้), `prayerLocation` (ที่ตั้งในการ์ดละหมาด)

---

## 6. 🔐 หน้าทีมงาน (สำหรับกรรมการกรอกข้อมูล)

มีหน้าพิเศษ **`team.html`** = ศูนย์รวมปุ่ม "เปิดฟอร์ม" ทั้งหมด ให้ทีมงานกดกรอกข้อมูลเข้าเว็บได้ง่าย ๆ
(เพิ่มเอกสาร / บันทึกการเงิน / เพิ่มประกาศ / เพิ่มคลังความรู้ / อัปเดตกิจกรรม / แก้กรรมการ / ฟอร์มสมัครสมาชิก)

**เข้าได้ที่:** เว็บจริงต่อท้ายด้วย `/team.html`
เช่น `https://muslimclub-kku.netlify.app/team.html`

> ⚠️ **เก็บลิงก์นี้เป็นความลับ** — หน้าเว็บจริงไม่มีปุ่มลิงก์มาหน้านี้ และตั้งค่าไม่ให้ Google ค้นเจอ
> เพราะใช้ Gmail ส่วนตัว (ตั้งฟอร์มให้เฉพาะคนในองค์กรกรอกไม่ได้) จึงใช้วิธี **"ซ่อนหน้า + เก็บลิงก์ไว้ในทีม"** แทน
> 👉 แนะนำ: ปักหมุดลิงก์นี้ไว้ในกลุ่มไลน์กรรมการ หรือบุ๊กมาร์กของทีม
> (ฟอร์มไหนยังไม่ได้ใส่ลิงก์ใน `config.js` จะขึ้นปุ่มเทา "ยังไม่ได้ตั้งค่าลิงก์")

**🔧 ลิงก์ "แก้ไขในชีต" (แนะนำให้ตั้ง):** ใส่ลิงก์ชีตของแต่ละระบบใน `config.js` บล็อก **`sheetEdit`**
(ใช้ลิงก์หน้าแก้ไขปกติที่ก๊อปจากแถบที่อยู่ตอนเปิดชีต — ไม่ใช่ลิงก์ csv) แล้วหน้าทีมงานจะได้เพิ่ม:
- ใต้ปุ่มเปิดฟอร์มทุกใบ มีลิงก์ **"กรอกผิด? แก้ไขในชีต"** — พิมพ์ผิด/อยากลบ ตามไปแก้ได้ทันที ไม่ต้องงมหาชีตเอง
- การ์ด **ตารางกิจกรรม** จะกลายเป็นปุ่ม **"เปิดชีต"** เขียวเต็ม (เพราะติ๊กสถานะ/แก้เดดไลน์ต้องทำในชีตอยู่แล้ว ฟอร์มทำไม่ได้)

---

## 7. 📱 ติดตั้งเว็บเป็น "แอป" บนมือถือ/แท็บเล็ต

เว็บนี้เป็น **PWA** = กดติดตั้งลงหน้าจอโฮมได้ ใช้งานเหมือนแอปจริง (เปิดเต็มจอ มีไอคอนชมรม) โดยไม่ต้องโหลดจาก App Store/Play Store

- **iPhone / iPad:** เปิดเว็บใน **Safari** → กดปุ่ม **แชร์** (สี่เหลี่ยมลูกศรขึ้น) → **เพิ่มไปยังหน้าจอโฮม**
- **Android:** เปิดใน **Chrome** → เมนู ⋮ → **ติดตั้งแอป / เพิ่มไปยังหน้าจอหลัก**

> ✅ ติดตั้งได้ต้องเปิดผ่าน **เว็บจริง (https://...)** เท่านั้น (เปิดไฟล์ในเครื่องเฉย ๆ ติดตั้งไม่ได้)
> ✅ เปิดได้แม้เน็ตหลุด (หน้าเว็บถูกเก็บไว้ในเครื่อง) — แต่ "ข้อมูลใหม่" จะอัปเดตเมื่อกลับมาออนไลน์
> หมายเหตุ: หน้าทีมงาน (`team.html`) ตั้งใจไม่ให้ติดตั้งเป็นแอป (เป็นหน้าส่วนตัว)

---

## 8. 🌐 อัปเดตเว็บขึ้นออนไลน์ (Deploy)

เว็บฝากไว้ที่ **Netlify** (ฟรี) วิธีอัปเวอร์ชันใหม่ขึ้นเว็บจริง:

1. เข้า https://app.netlify.com → เลือกเว็บไซต์ **muslimclub-kku**
2. ไปแท็บ **Deploys**
3. **ลากทั้งโฟลเดอร์** `Muslim Club Website` ไปวางในกรอบ "Drag and drop your site output folder here"
4. รอสักครู่ เว็บจริงจะอัปเดต ✅

> 💡 ในโฟลเดอร์ของคุณมี `js/config.js` (ข้อมูลจริง) อยู่แล้ว — ลากขึ้นไปได้เลย ข้อมูลจะครบ
> 💡 ถ้าแก้แล้วเปิดเว็บไม่เห็นเปลี่ยน ให้กด **Ctrl + Shift + R** (ล้างแคชเบราว์เซอร์)

---

## 9. 🔔 ระบบแจ้งเตือนเข้ากลุ่มอัตโนมัติ

ให้ระบบเด้งเข้ากลุ่มกรรมการเองเมื่อ **มีประกาศใหม่** และ **สรุปเดดไลน์งานฝ่ายทุกเช้า 08:00** — รองรับ Telegram / Discord / LINE OA

ติดตั้งครั้งเดียว ~15 นาที ไม่ต้องเปิดคอมทิ้งไว้ (Google รันให้บนคลาวด์)
→ วิธีทำทั้งหมดอยู่ใน **`google-apps-script/วิธีติดตั้งแจ้งเตือน.md`**

---

## 10. 🤝 ส่งมอบรุ่นถัดไป

กรรมการเปลี่ยนทุกปี — มีเช็คลิสต์ส่งมอบบัญชี/ไฟล์/ขั้นตอนขึ้นปีการศึกษาใหม่ รวมถึงวิธีอัปเกรดเป็น**โดเมนจริง + อีเมลชมรม** อยู่ใน **`docs/คู่มือส่งมอบรุ่น.md`**
